<%= @host.email %>

<% @host.comments.each do |c| %>
    <%= c.writer %>
    <%= c.content %>
<% end%>

<%= form_for Comment.new, url:host_comments_path(@host.id) do |f| %>
    <%= f.label "댓글" %>
    <%= f.text_field :content %>
    <%= f.submit "작성" %>
<% end %>